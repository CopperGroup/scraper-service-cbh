// src/utils/constants.js

// Define constants for scrape statuses
const SCRAPE_STATUS = {
    QUEUED: 'queued',
    SCRAPING: 'scraping',
    SCAPED: 'scraped', // Note: Typo in original request, assuming 'scraped'
    FAILED: 'failed'
  };
  
  module.exports = {
    SCRAPE_STATUS
  };